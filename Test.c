#include <stdio.h>
#include <string.h>
extern int factorial(int n);

long add(int a, int b)
{
    return a + b;
}

int main()
{
    char *a = "a";
     char *b = "a";
    printf(struct test{32, "hello world" });
}
