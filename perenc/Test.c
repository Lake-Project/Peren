#include <stdio.h>
extern char charat(int i);

int main()
{
    for(int i = 0; i < 11; i++)
    {
        printf("%d \n", charat(1));

    }
    // char* test = "a";
    // char a = test[0];
    // printf("%c \n", a);
}
